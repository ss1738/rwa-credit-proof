You're taking over grant-submission prep for an existing project. Read this whole prompt before doing anything. The repo is already on this machine at ~/rwa-credit-proof (GitHub: https://github.com/ss1738/rwa-credit-proof, branch main, already pushed and in sync, git identity already correctly configured as "Satyawan Singh <satyawansinghinuk@gmail.com>", working tree clean as of this handoff). Do not assume you need to clone anything, it's already local.

## What this project is

A working, audited zero-knowledge proof-of-solvency system for tokenized private credit. A fund proves it's solvent and fully KYC'd over a private loan book, without revealing a single loan, and a token only mints when the ZK proof plus the servicer's signature both check out. This is NOT vaporware: every claim is independently reproducible by running `./demo.sh` in the repo root, which was actually run and verified this session:
- Circuit has 5,911 real constraints; insolvent/KYC-failed/wrong-commitment books all correctly fail
- On-chain verification on a LOCAL Solana test validator: 83,352-83,354 compute units (~6% of the 1.4M cap), constant regardless of book size
- Same proof is deployed and LIVE on public Ethereum Sepolia testnet: contract `0x8c3DD5E6b660D6aFdCdCa2FB757b2E777d8511Df`, verify tx `0x91196bd0a9b6d192733bdc7df7126520c142f2bbda2c146190ebddd75aaae7c7`, publicly inspectable on sepolia.etherscan.io
- Known limitations are honestly documented in KNOWN_LIMITATIONS.md (main open item: the Groth16 trusted setup is currently single-party, needs a multi-party ceremony or a transparent proof system for a real soundness claim)

Repo layout you should know about:
- `README.md` has a "Related work" section comparing this project against Zyga (SOL Strategies), the closest competitor. Keep that positioning consistent in anything you write.
- `AUDIT.md` and `KNOWN_LIMITATIONS.md` are the honesty record, read them before writing any claims.
- `GRANT_APPLICATION.md`, `SUPERTEAM_APPLICATION.md`, `SUPERTEAM_UK_APPLICATION.md`, `COLOSSEUM_SUBMISSION.md` are draft pitch docs from a prior session, written as long-form milestone documents. See "Task 2" below, they need to be reshaped.
- `demo.sh` reproduces everything end to end. Rust toolchain and Solana CLI (`solana-cli 4.1.1`) are already installed on this machine.
- The whole repo (all tracked .md/.html files) has a strict no-em-dash rule ("public-copy rule", see commits 7db68b9, 31ce421, 1d85bb9). Never use — (U+2014) anywhere in this repo. Use commas, colons, periods, or restructure the sentence instead. En-dashes in number ranges (e.g. "$1.8-3.14T") are fine.

## The situation

The user previously submitted a Solana Foundation grant application for **$300,000** and it was rejected: "the dollar amount requested exceeds the value of the public good delivered." A prior session (me, in an earlier context) rescoped the ask down to $20,000-$25,000 across two narrow, protocol-agnostic milestones (open-source dev kit + a multi-party trusted-setup ceremony), removing a paid single-protocol-integration milestone that likely caused the original rejection. That rescoping is written up in `GRANT_APPLICATION.md`.

However: **that rescoped document is the wrong shape.** It was written as a long milestone-by-milestone proposal, but the actual Solana Foundation application is a short web form (a HubSpot form linked from solana.org/grants-funding), not an email or a document upload. This session verified the real form fields directly by opening the form. They are exactly:

1. Company name* (for a solo/unincorporated applicant, likely just "Satyawan Singh", matches how the rejection email addressed the applicant: "Satyawan Singh (solo developer, unincorporated)")
2. Website URL* (currently no dedicated site exists, only the GitHub repo, see Task 3 below)
3. Country* (United Kingdom)
4. First Name*
5. Last Name*
6. Email Address*
7. Solana On-Chain Accounts* ("Insert this project's on-chain addresses (program ID, token address, fee payer wallets etc.). Separate them using a comma.") **THIS IS CURRENTLY UNFILLABLE, see Task 1, it's a hard blocker.**
8. Funding Amount* (a single number, not a range, they want a specific figure)
9. Funding category* (dropdown: AI / Consumer / DeFi / Developer Education & Ecosystem / Developer Tooling). "Developer Tooling" is the right choice given the rescoped narrow, reusable-primitive framing, not "DeFi" (the guidelines explicitly say purely commercial projects don't qualify for a standard grant, and Developer Tooling framing avoids that trap).
10. Why You?* ("Why are you and your team the best people to build this project? What edge do you have over the competition?") — single free-text field, not a milestones/budget breakdown.

There is no field for milestones, budget breakdown, or public-good justification on the form itself, those get discussed later if the Foundation schedules a due-diligence call. Do not write a long document expecting it to be pasted somewhere; the form fields above are it.

Also checked: **Superteam UK's application is NOT a standalone form you can fill in outside their platform.** Submission happens through a logged-in Superteam Earn profile (superteam.fun/earn) with KYC required, and applications for the UK-specific grant were showing as "Applications Paused" at time of writing (check current status before doing anything with it). Their public guidance doc (linked from the listing) requires Google Docs edit-access to read and wasn't fully accessible this session. Do not attempt to create an account or log in on the user's behalf, that's off-limits, tell the user to do that part themselves when the program reopens.

## Your tasks, in order

### Task 1: Get a real Solana on-chain address (blocking)
The Solana verifier program (`solana-verifier/` in the repo) has only ever been tested against a LOCAL `solana-test-validator`, it has never been deployed to a real network. Deploy it to **Solana devnet** (free devnet SOL via `solana airdrop`, no real funds involved, this is standard practice and low-risk):
1. `solana config set --url devnet`
2. Generate or reuse a devnet keypair, airdrop devnet SOL (`solana airdrop 2`, may need to retry if rate-limited)
3. Build the on-chain program: `cd solana-verifier && cargo-build-sbf` (same command `demo.sh` stage 6 already uses locally, check that script for the exact working invocation and env vars, e.g. `SBF_OUT_DIR`)
4. Deploy: `solana program deploy target/deploy/<program>.so`
5. Verify it on-chain: send a real proof-verification transaction against the deployed devnet program (adapt the existing `client/` RPC client, which already knows how to send a real proof tx to a deployed program, check `client/src` for how it currently targets things)
6. Record the resulting program ID and the verification transaction signature. These go into the "Solana On-Chain Accounts" field and should also be added next to the existing Sepolia deployment info in README.md, AUDIT.md, and wherever else the repo currently only cites the local-test-validator measurement, so the repo's own story is "live on devnet AND live on Sepolia," not "local-only on Solana."
7. Update GRANT_APPLICATION.md and any other pitch doc that currently says "local Solana test validator" to also cite the new devnet deployment where relevant. Don't remove the local-validator CU measurement, it's still the most precise compute-unit figure, just add the devnet deployment as the "real on-chain account" evidence.

If devnet deployment turns out to be non-trivial (e.g. the program needs upgrade authority setup, or the verifier's account model doesn't map cleanly to a single deployable program), figure out the smallest true thing that satisfies "Solana On-Chain Accounts": even a single deployed, callable devnet program ID is enough, it doesn't need to be a polished product deployment.

### Task 2: Reshape GRANT_APPLICATION.md into the real form's shape
Rewrite `GRANT_APPLICATION.md` (or create a new file, your call, but make sure whichever is authoritative is obvious and doesn't leave a stale duplicate) as **direct, ready-to-paste answers to the 10 fields listed above**, not a milestone document. Guidance per field:
- Company name: "Satyawan Singh" (confirm with the user if they'd rather use something else, don't just assume)
- Website URL: use the GitHub repo URL unless Task 3's landing page is done, then use that domain instead
- Solana On-Chain Accounts: the devnet program ID + any other relevant addresses from Task 1, comma-separated, exactly as the form wants
- Funding Amount: pick ONE number, not a range. The previously-researched calibration was $20,000-$25,000 total across two milestones; for a single-number field, $22,000 is a reasonable midpoint, but use your judgment, and note that the milestone breakdown ($5-7k dev kit, $15-18k ceremony) can still live in a short internal note for the user's own reference even though the form doesn't ask for it directly.
- Funding category: "Developer Tooling"
- Why You: pull from the existing "Team" section already in the repo's pitch docs (from-scratch Rust L1 blockchain: consensus, light clients, data-availability sampling; BLS12-381 aggregate signatures; Nova recursive SNARKs; Groth16; KZG/Verkle commitments; on-chain BLS EIP-2537; Coq/TLA+ formal verification), plus the fact that, unlike most applicants, this ships with a fully reproducible, independently-auditable working system today (AUDIT.md, the `./demo.sh` one-command reproduction, live deployments on two chains after Task 1), not just a plan. Keep it tight, this is a single field, not an essay. No em-dashes.

Keep the existing "Related work" (Zyga) positioning and the honest-limitations framing somewhere accessible (either folded briefly into "Why You" or just left in the README for anyone doing due diligence to find), don't lose that work.

### Task 3: Project landing page (only if the user gives you a domain)
The user mentioned owning a domain they want to use for the "Website URL" field instead of a bare GitHub link, but hadn't given the actual domain name before this handoff. Ask them for it before doing anything here. Once you have it:
- Recommended approach: **GitHub Pages**, deployed from this repo, since it needs no third-party hosting credentials from the user at all, just one CNAME DNS record they add themselves at their registrar. Do not ask for or accept registrar/hosting account passwords, if a task seems to require login credentials, stop and ask the user to do that specific step themselves.
- Build the page from what already exists in the repo (`EVIDENCE.html` is a strong starting point: it already has the measured-results table and the live Sepolia deployment details) rather than starting from scratch. Keep the no-em-dash rule. Add the devnet deployment info from Task 1 once it exists.
- Add a `CNAME` file to the repo with the domain, enable GitHub Pages in repo settings (via `gh` CLI if authenticated, or tell the user the one manual settings-page step if not), and tell the user exactly what DNS record to add and where.

### Task 4: Sanity pass before telling the user it's ready
Before saying anything is submission-ready:
- Run `./demo.sh` yourself and confirm all 6 stages still pass (this project's own convention: never claim a number you haven't reproduced).
- Grep the whole repo for em-dashes (`grep -rn "—" --include="*.md" --include="*.html" .`) and fix any you introduced.
- Cross-check any new numbers you cite (devnet program ID, funding amount, CU/gas figures) are consistent across every file that repeats them, this codebase has a history of catching exactly this kind of drift (see commit history around wording/figure corrections).
- Confirm with the user before pushing anything to GitHub (they've been explicit throughout this project that pushes happen only on direct request, not automatically after every commit), and never force-push without explicit confirmation of exactly that.

## Things NOT to do
- Don't touch Kelvotem (a separate, unrelated company/project, off-world infrastructure, nothing to do with this repo).
- Don't create a Superteam Earn account, log into one, or otherwise act on the user's behalf on any platform requiring authentication.
- Don't update global git config (the user's own machine identity is already correctly set, `Satyawan Singh <satyawansinghinuk@gmail.com>`, verify with `git config --global user.name/user.email` if you need to confirm, but don't change it).
- Don't rewrite already-pushed commit history unless explicitly asked; if you do (e.g. to fix an authorship mistake), get explicit confirmation first since it requires a force-push.
