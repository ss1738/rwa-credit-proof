You're taking over developing the public website for this project. Read this whole prompt before doing anything. The repo is already on this machine at ~/rwa-credit-proof (GitHub: https://github.com/ss1738/rwa-credit-proof, branch main, pushed and in sync, git identity already correctly configured as "Satyawan Singh <satyawansinghinuk@gmail.com>"). Do not clone anything, it's already local.

## Current state of the site (as of this handoff)

- **Live at:** https://caledren.com and https://www.caledren.com, HTTP 200, confirmed working.
- **Hosting:** Vercel, team `satyawansinghs-projects`, project `rwa-credit-proof`. The Vercel CLI is already installed globally on this machine and already logged in as `satyawansinghinuk-stack`. Run `vercel whoami` to confirm before doing anything, don't re-run `vercel login`, it's already authenticated.
- **How the domain got here:** `caledren.com` previously belonged to a different, unrelated Vercel project called `personal-bank-pdf` in the same account. It was moved to `rwa-credit-proof` at the user's explicit, twice-confirmed request this session. `personal-bank-pdf` itself was not touched or deleted, it's just no longer attached to this domain. **Do not touch `personal-bank-pdf` or any other project in this Vercel account** (there are ~20 others: kelvotem-website, elyzion-web, velkron-site, zovonotes-website, fingaurd, etc, all unrelated to this one). Only ever act on the `rwa-credit-proof` project.
- **Current deploy mechanism (needs fixing, see Task 1):** the live site was deployed by copying `index.html` out of this repo into a scratch directory and running `vercel deploy --prod` from there. There is currently NO proper link between the GitHub repo and the Vercel project, deploys are manual and disconnected from version control. Fix this before doing anything else.
- **Current site content:** `index.html` at the repo root is a byte-for-byte copy of `EVIDENCE.html` (a technical evidence sheet: measured results table, audit summary, honest limitations). It is functional but minimal, a single unstyled-beyond-basics page, not a real developed website. That's what "upgrade and develop" means here.

## What this project actually is (context for writing good copy)

A working, audited zero-knowledge proof-of-solvency system for tokenized private credit. A fund proves it's solvent and fully KYC'd over a private loan book, without revealing a single loan, and a token only mints when the ZK proof plus the servicer's signature both check out (currently that combined check is modeled natively/off-chain, not yet enforced by the deployed on-chain program, see the scope-correction note below, this matters for what you can honestly claim).

Read these files before writing any copy, they are the single source of truth, don't invent facts beyond them:
- `README.md`, especially the "Related work" section (positions this project against Zyga, a SOL Strategies product: general/commercial/institutional vs. this project's narrow/open-source/already-working framing). Keep that positioning if you reference competitors.
- `AUDIT.md` and `KNOWN_LIMITATIONS.md`, the full honesty record.
- `DEPLOYMENTS.md`, per-network deployment status with real evidence. As of this handoff: Solana devnet deployment is still PENDING (blocked on faucet rate-limiting, not yet resolved), local Solana validator is confirmed, Ethereum Sepolia is live and public (contract `0x8c3DD5E6b660D6aFdCdCa2FB757b2E777d8511Df`). Check this file fresh, devnet status may have changed since this handoff, if it now shows a real devnet deployment, the website should say so.
- `DESIGN_PARTNER_BRIEF.md`, the actual ask this project has for outside parties (one design partner, free integration, in exchange for a real loan tape to pilot against). If the site has any kind of "get involved" or contact section, ground it in this, not a generic "book a demo" template.
- `evidence/2026-09-17/` has real logs/receipts backing every number. Link to specifics rather than restating bare numbers where it strengthens credibility.

**Critical scope correction, do not contradict this anywhere on the site:** the deployed Solana on-chain program (SBF) verifies the Groth16 pairing equation only. It does NOT pin an approved circuit/key, does NOT check the servicer's signature, and does NOT mint tokens on-chain. The combined proof+signature+mint-gate model currently exists only in the native/host-side demo (`src/onchain.rs`), not the deployed program. Every page of this repo's docs was carefully corrected to say this precisely, session before this one. Do not regress to the older, looser phrasing ("the mint is gated on both the proof and the signature" without qualification) anywhere on the website.

**No em-dashes anywhere** (— U+2014). This repo has a strict, established rule against them in all public-facing copy (see commit history: 7db68b9, 31ce421, 1d85bb9, and the audit commit from this session). Use commas, colons, periods, or restructure the sentence. En-dashes in number ranges are fine.

## Your tasks

### Task 1: Fix the deploy pipeline first, before touching design
Right now the live site isn't connected to this git repo at all, someone could edit `index.html`, forget to redeploy, and the live site would silently drift from what's in version control. Fix this properly:
1. `cd ~/rwa-credit-proof && vercel link` to link this repo directory to the existing `rwa-credit-proof` Vercel project (team `satyawansinghs-projects`). This creates a `.vercel/` directory, add it to `.gitignore` if it's not already ignored (check first, don't duplicate an existing entry).
2. From then on, deploy with `vercel deploy` (preview) or `vercel deploy --prod` (production) run directly from the repo root, no more copying files to a scratch directory.
3. Decide whether you want full CI (auto-deploy on every push to `main` via Vercel's GitHub integration) or to keep deploys manual-but-repo-linked. If you set up the GitHub integration, do it through `vercel git connect` or note that the one manual step (connecting the GitHub repo in the Vercel dashboard's project settings) needs the user to click through it themselves if it needs a GitHub App installation permission grant, don't try to force that through the API if it requires an interactive OAuth-style approval.
4. Whatever you decide, document the actual deploy command in `README.md` (a short "Website" or "Deploying the site" section) so this doesn't get lost again.

### Task 2: Decide what belongs on a real static site vs. stays as a repo file
`index.html` currently duplicates `EVIDENCE.html`. Don't just keep editing both in parallel, that's how they drift. Recommended approach:
- Keep `EVIDENCE.html` as the print-to-PDF technical evidence sheet, it's referenced from other docs (`GRANT_APPLICATION.md`, `SUPERTEAM_APPLICATION.md`, etc.) as a specific artifact, don't remove it or change what it's for.
- Make `index.html` (or a proper multi-file site if you restructure, e.g. `site/` directory built with something like Astro/plain HTML+CSS/whatever is proportionate, don't reach for a heavy framework for what is fundamentally a handful of static sections) the richer public-facing page, sourcing its facts from `DEPLOYMENTS.md` and `KNOWN_LIMITATIONS.md` rather than hand-copying numbers that will go stale. If you introduce a build step, make sure `vercel deploy` still works with it (add a `vercel.json` or let Vercel's framework detection handle it, verify with an actual preview deploy that it builds correctly before promoting to production).

### Task 3: Actually develop and upgrade the site
This is intentionally open to your judgment on structure and visual design, but ground every section in the repo's real content, don't invent traction, customers, or metrics that don't exist (`DESIGN_PARTNER_BRIEF.md` explicitly states there's no design partner yet, `DEVELOPER_TOOLING_PROPOSAL.md` explicitly states no production customers or usage metrics are claimed, respect that). Reasonable sections to consider, use your judgment on which earn their place:
- A clear hero/opening statement of what the project proves (the one-liner already used consistently across the repo's docs: "A fund proves it's solvent and fully KYC'd, without revealing a single loan.")
- The measured-results table (already exists in `EVIDENCE.html`, keep the same honest phrasing, don't drop the "SBF program verifies pairing only" caveat when you move it)
- The three-way audit summary (empirical adversarial audit + two independent code reviews)
- Honest limitations, prominently, not buried, this repo's whole credibility rests on not hiding this
- Related work / Zyga positioning, if you keep a competitors section, keep it factual and not dismissive
- A real, honest "get involved" section pointed at design partners per `DESIGN_PARTNER_BRIEF.md`'s actual ask, a plain `mailto:` link is enough, don't build a fake lead-gen form backed by nothing
- Links to the repo, the demo, and the specific evidence files/transaction hashes, real proof beats a marketing claim every time here

Visual design: this is a credibility-first technical/finance audience (Solana Foundation reviewers, RWA protocol engineers, ZK people who will actually check your claims). Don't over-design it into something that reads as hype. Clean, typographically careful, restrained color use, evidence-forward layout (tables, code blocks, real transaction links) will serve this audience better than a flashy crypto-marketing template.

### Task 4: Sanity pass before calling anything done
- Grep the whole repo for em-dashes after your changes: `grep -rn "—" --include="*.md" --include="*.html" .`
- Cross-check every number/claim you put on the site against `DEPLOYMENTS.md`/`KNOWN_LIMITATIONS.md`/`AUDIT.md`, this repo has a strong track record of catching exactly this kind of drift, don't be the session that introduces it.
- Deploy to a PREVIEW first (`vercel deploy`, no `--prod`), check the preview URL actually renders correctly, then promote to production (`vercel deploy --prod` or `vercel promote <preview-url>`) only after that check. Don't push straight to the production domain without having looked at it.
- Confirm with the user before pushing repo changes to GitHub, and before promoting a preview to production on the live domain, this is a real public-facing site now, not a draft.

## Things NOT to do
- Don't touch any other Vercel project in this account, especially not `personal-bank-pdf`. If you need to check what else exists, `vercel projects ls` is read-only and fine, don't modify anything on the list except `rwa-credit-proof`.
- Don't ask the user for a Cloudflare API token, a Vercel token, or any other credential. Vercel CLI is already logged in on this machine, DNS is already correctly pointed, nothing further is needed. If a tool suggests using an env var token as a workaround, ignore that suggestion.
- Don't touch Kelvotem or any other unrelated project/repo.
- Don't update global git config.
- Don't invent metrics, customer names, or adoption numbers that aren't in the repo's own docs.
- Don't rewrite already-pushed commit history, or force-push, without asking first.
